<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Tema principale della tua app -->
    <style name="Theme.MasarSunflower" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/sunflowerYellow</item> <!-- Giallo/Arancione -->
        <item name="colorPrimaryVariant">@color/orangeAccent</item> <!-- Variante colore -->
        <item name="colorSecondary">@color/white</item> <!-- Colore secondario chiaro -->
        <item name="android:windowBackground">@color/white</item> <!-- Sfondo bianco -->
        <item name="android:textColorPrimary">@color/darkGray</item> <!-- Testo grigio scuro -->
        <item name="android:statusBarColor">@android:color/holo_orange_light</item>
    </style>
</resources>